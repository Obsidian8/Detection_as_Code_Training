

name: TOML/MITRE Validation

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4


      - run:
          python detections/development/validation.py
          python detections/development/mitre.py
